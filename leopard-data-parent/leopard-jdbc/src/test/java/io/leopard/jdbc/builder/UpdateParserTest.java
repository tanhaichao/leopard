package io.leopard.jdbc.builder;

import org.junit.Test;

public class UpdateParserTest {

	@Test
	public void getFields() {
		// UpdateParser parser = new
		// UpdateParser("update table set nickname=? where username=?;");
		// LinkedHashSet<String> set = parser.getFields();
		// System.out.println("set:" + set);
		// Assert.assertEquals("[nickname, username]", set.toString());
	}
}