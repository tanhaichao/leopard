package io.leopard.exporter;

/**
 * 字段类型
 * 
 * @author 谭海潮
 *
 */
public enum FieldType {
	STRING, INTEGER, BOOLEAN, DATE, ONLY_DATE, TIMESTAMP, DOUBLE, FLOAT, LONG, SNUM, INUM, BNUM, MONTH, BIT, JSON
}
